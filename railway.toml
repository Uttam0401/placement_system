
[build]
builder = "nixpacks"
buildCommand = "./mvnw clean package -DskipTests"

[deploy]
startCommand = "java -jar target/placement-system-1.0.0.jar"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3